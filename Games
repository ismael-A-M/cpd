t=int(input())
h=[]
for i in range(t):
    x,y=map(int,input().split())
    h.append([x,y])
C=0
for l in range(t):
    for m in range(t):
        if m==l:
            continue
        elif h[l][0]==h[m][1]:
            C+=1
print(C)

