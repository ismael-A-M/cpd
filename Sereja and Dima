t=int(input())
arr=list(map(int,input().split()))
h=0
c=0
for i in range(1,t+1):
    if i%2==0:
        m=max(arr[0],arr[-1])
        c+=m
        arr.remove(m)
    else:
        m=max(arr[0], arr[-1])
        h+=m
        arr.remove(m)
print(h,c,sep=" ")
